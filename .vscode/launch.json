{
    "configurations": [
        {
            "name": "Python",
            "type": "debugpy", //am i supposed to switch to python //was "debugy"
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${fileDirname}"
        }
    ],
}